service firebase.storage {
  match /b/andev-app.appspot.com/o {
  	match /{year}/profiles/{profileId} {
    	allow read: if true;
    	allow write: if request.resource.contentType.matches('image/.*');
    }
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
